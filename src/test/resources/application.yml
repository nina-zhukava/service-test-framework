spring:
  profiles: common
  main:
    allow-bean-definition-overriding: true

test:
  service:
    host: http://taschool-notes-service.herokuapp.com

auth:
  token: Basic dGVzdDpzZWNyZXQ=
  client:
    id: test
  grant:
    type: password